name: era_flutter
description: Era visitor continuity and verification for Flutter applications.
version: 0.1.0
repository: https://github.com/season-labs-inc/intent-scoring

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  shared_preferences: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
